import { Text, View } from "react-native";

export default function prayerTimes() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>This is the prayer times</Text>
    </View>
  );
}
